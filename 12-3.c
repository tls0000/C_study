#include <stdio.h> 
#include <malloc.h>

void main()
{
	int* p;
	int i, hap=0; 
	int cnt;

	printf(" ÀÔ·ÂÇÒ °³¼ö´Â?");
       	scanf("%p),
		
}d", &cnt);
	*
		* p = (int)malloc(sizeof(int) cnt),
		* for(i=0; i<cnt; i++)
			* for(i=0
					* {
					* }
					* ¿¥ºê
					* printf("%d | I¥ð ", i+1); scanf("%d", p+i);
					* for(i=0; i<cnt; i++)
					* hap hap + (p+i);
					* FO<<
						* printf("ÎáÎ
